# Archived Setup Scripts

`setup_server.old-debian-ubuntu.sh` is copied from:

```text
/home/lfy/my_scripts/linux/cloud/setup_server.sh
```

It is kept as historical reference only. It was written for Debian/Ubuntu and
must not be run on Fedora.
